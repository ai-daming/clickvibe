# 修复纪律与并发不变量(fix discipline)

> 本文档从 issue #111(十轮,调用点补丁循环)与 #144(八轮,概念膨胀循环,见原则 11)的 review 循环复盘提炼。它不是风格偏好,而是约束:凡涉及状态、并发、持久化的修复,违反即返工。配套的 review 流程见 `skills/root-cause-review/SKILL.md`。

## 为什么需要这份文档

#111 用十轮 review 修一类并发 bug,每轮 CRITICAL 都被"精确修掉",但累计 diff 从 93 行涨到 2669 行,问题从未消失。复盘结论:

> 每一轮修的都是"reviewer 上次看到的那个具体交错",而不是"让所有交错都不可能的机制"。补丁在调用点加检查,每个检查点又成为新的竞态窗口——bug 空间随补丁数增长,循环永远追不上。

十轮母题归类(同一 bug 的十次变装):

| 轮 | 表面问题 | 母题 |
|---|---|---|
| R1 | 无证据判 interrupted;裸 catch 兜底 | 证据模型错 |
| R2 | stage 先推进、任务后 settle → 任务不可见 | 多事实源各执一词 |
| R3 | taskId 早于进程启动 + registry 空 → 推断已死 | 用缺失证明死亡 |
| R4-R6 | 面板/命令/服务端各自推导"当前任务是谁" | 多处重复推导 |
| R7-R8 | 校验只覆盖上轮被点名的分支/状态 | 不变量按样本打补丁 |
| R9-R10 | 迟到回调覆盖后继;check-then-write 不原子 | 写入无互斥、无原子性 |

R4-R8 是同一件事(重复推导不一致),R9-R10 是另一件事(写入无并发控制)浮出水面。**同类问题连续多轮复发 + diff 持续发散 = 修错了层面,必须停机重设计。**

## 代码级原则(六条,触发式)

### 1. 不变量先行

修任何状态/并发 bug 前,先写下不变量:谁拥有事实的唯一所有权?什么是原子提交单元?哪一代次持有写权限?查无记录能推出什么、不能推出什么?

答不出不变量 = 还没理解问题,**禁止动手写修复**。不变量写在修复 PR 的描述或 issue 评论里,让 review 可以对着它检查。

### 2. 构造优于纪律

在 N 个调用点插入"先检查再操作"不是修复,是登记债务:每个检查点本身就在竞态窗口里,且要求未来所有调用者记得做对。

判断一个修复的层次:
- **机制级**:不变量由类型、存储原语或串行化点强制,违反者写不出来。
- **调用点级**:靠每个调用处自觉 check。允许作为过渡,但必须登记收敛期限。

倾向前者的具体形态:把校验收进被调用的原语内部(而非调用方)、把所有权做进类型(非法状态不可表示)、单写者 + 原子提交。

### 3. 单一应答源

任何"当前 X 是哪个"的问题(当前任务、当前代次、当前分支、当前基线)全系统只允许一个应答源。面板、对话命令、API、后台回调一律消费同一引用,**禁止各自从 stage/历史/内存重新推导**。出现第二份推导逻辑 = 缺陷,即使当前两份恰好一致。

### 4. 写权限即凭证

变更共享持久状态的 API 必须在**签名上**要求代次/所有权凭证,校验发生在串行化临界区**内部**(先取得互斥,再读当前代次比对,再写),而不是临界区之前先 check 再写——后者只是把竞态窗口从"写之前"挪到"check 与写之间"。

反例(#111 R10):`saveCurrentTaskWorkflow` 先 load 校验、再用非原子 writeFile 覆盖,后继任务在两者之间落盘,旧回调 17% 概率覆盖新状态、11% 概率写坏文件。

### 5. 缺失 ≠ 死亡

内存表/registry 查无记录只能推出"当前控制器不可见"(unknown),不能推出"已终止"(interrupted)。unknown 必须 fail closed(禁止启动新任务以防双开),但**禁止宣判死亡**;只有明确证据(用户确认停止、进程退出码、supervisor 终态、超时)才允许写入终态结论。

禁止用任何单一信号冒充存活/死亡证明:PID 存在、日志时间戳新鲜、本地游标推进,都只是组合证据的一部分。

### 6. 对抗性验证与静态枚举审计

验证分两层,缺一不可:

- **静态枚举审计(先做)**:对每条不变量,grep 枚举全部可能违反它的代码路径(mutation API 的所有调用方、应答源的所有消费方、所有写入口),产出逐条打勾的枚举表。存在性缺口(哪些路径绕过了机制)是静态可枚举的,**必须一次找全**,一次找一个 = 跳过了这层。任何"靠调用点纪律"或"无保护"的行即为 finding,不需要动态复现。
- **动态对抗验证(后做)**:与实现同一心智模型的测试不是覆盖。并发修复的回归测试必须包含实现者**没想到的交错**:统计压测(高并发循环 + 失败率断言)、随机调度,或明确构造危险窗口(如"校验完成与写入之间插入后继写")。枚举表只证明路径覆盖,证明不了临界区内部逻辑与锁语义的正确。

纯函数输入输出测试证明不了跨实例、跨刷新、跨生命周期的正确性。CRITICAL 级 finding 的验证必须执行复现,不得只读代码推断;完备性的验收标准是**枚举表全部打勾**,不是"reviewer 没找到问题"。

## 流程元规则(两条,给循环)

### 7. diff 收敛律

连续修复轮的 diff 应收敛或稳定。修复轮持续净增代码(本例 93→885→1316→…→2669)= 在调用点层面堆补丁的确定性信号。review 见到发散趋势应升级处理,而不是继续逐条验证。

### 8. 同类停机律与两级升级

同一母题的 CRITICAL 连续 ≥2 轮出现(本例 R7-R10 连续 4 轮),循环必须停止"逐条修复"模式,切换为"重设计轮":产出半页不变量文档 + 机制级方案,经确认后再实现。dev 侧不会自己喊停(它的激励是过本轮 review),停机判定必须在 review 侧。

升级有两级,判断链条到此为止:

- **实现层升级**(默认):同类复发 → 修法不是调用点级 → 换机制级方案。
- **抽象层升级**(终止条件):同类母题在**机制级修复之后**仍然复发 → 不是实现不完整,是模型选错了。下一轮的产出不是再加一层围栏,而是质疑共享模型本身(如"单文件整对象快照写"这个前提),给出替代抽象。在替代抽象上继续打补丁,只会得到第 N+1 层洋葱。
- **升级前置检查**:判定"抽象错"之前,必须先确认上一轮的方案**真的落地了**——对比改造前后的公开 API/导出面,排除"改名式修复"(旧能力换名存活,#111 R17 实例)。"没做完"与"做错了"的处理完全不同:前者补完,后者重设计;混淆二者会让循环在两种判决间震荡,永不收敛。

### 10. 可恢复性优于预防(2026-08-25,#90 现场)

**"对"不是"不出错",是"错了能回来"。**防护应只加在**不可恢复**的伤害上(双开任务、状态损坏、不可逆删除);可恢复的工作(冲突解决、代码修改、会话恢复)应允许 agent 直接尝试——恢复网由 git 历史 + review 绑定新 HEAD + 全部门禁 + 显式合并策略构成,已把爆炸半径圈死。并行开发必然产生大量合并冲突,按冲突人工介入等于杀死并行承诺;因"可能改坏"而拒跑 resume 属于把预防花在可恢复之事上(#90 曾因此每次重挂即死循环,且被谎标 session-interrupted)。原则 5 的 fail-closed 同理只约束不可恢复的动作。

### 9. 已知模式先验(先查地图,再推导)

并发/状态 bug 的根因绝大多数落在已知模式库里。定位根因时**先模式匹配、再自行推导**:

| 症状 | 已知模式 | 关键词 |
|---|---|---|
| 串行化了但仍被旧数据覆盖 | lost update(原子地丢更新) | CAS / expected revision |
| 旧持有者迟到写覆盖新持有者 | fencing token / lease(代次凭证) | Kleppmann《DDIA》第 8 章 |
| 检查与动作之间窗口 | TOCTOU,校验必须进临界区 | check-then-act |
| 半写状态可见 | torn write | temp + rename 原子提交 |
| 多处对"当前是谁"答案不一致 | 单一事实源缺失 | single writer / 单一应答源 |
| 多个排序域互不知晓(回调队列 vs 控制器动作) | 单写者命令日志 / actor | 全部生命周期动作进同一串行域;fencing token 只是它的组成部分,不是完全体 |

#111 的 R9-R16 用八轮从零推导出了 fencing token 的完整形态——该结果在教科书里已存在。推导之前先问:"这是不是上表里的某个已知问题?"匹配则直接按模式的完整形态实现(一步到底,不做半成品),不匹配再进入自行推导。

### 11. 概念预算(2026-08-29,#144 八轮现场)

#111 的病是**调用点补丁**(向下堆检查);#144 暴露的是对偶的病:**概念膨胀**(向上加层)。八轮 review 里每轮 finding 都是真的、每轮也都"修掉了",但修法持续是新增概念——直到 Observer 干预剃掉约 500 行,行为零变化,问题一个没回来。八轮母题:

| 轮 | 表面问题 | 当轮新增的概念 | 其实该做的 |
|---|---|---|---|
| R1-R2 | 单飞跨代 join、反序完成混元数据 | 信封层 + 发布 CAS(正当,机制补齐) | —(非膨胀) |
| R3 | branch count 失败折叠成 hasCommits:false | 33 行 section 契约表 + 消费表的生成器 harness | 解析器里补一条 required 判断 |
| R4 | worktree/branch 两套基线解析 | `EB_*` 四段 + 两个 `*_BASE_REF` 标签段(六段里五段的值从未被读) | 一个共享变量(最终收敛为 `EFFECTIVE_BASE` 一段) |
| R5 | configured 非 git 仓库伪装"分支不存在" | `BR_GITDIR` canary | 已有的 rc=1/rc>1 区分(同一不变量已有机制) |
| R6 | 枚举采样未继承不可变/证据机制 | 第三张契约表 + 平行的一层 | 复用已有的冻结/序列化代码 |
| R7 | Observer:并行表达同一契约 | `observe*` 原语(正当,且是首次做减法) | —(收敛起点) |
| 剃刀 | — | —(纯删除:表、harness、五个顾问段、canary、行级镜像,−500 行,行为零变化) | — |

膨胀信号**不是 diff 行数**(#144 行数增长大部分正当),是**新名词增长率**。#144 每轮都自称"机制级修复",原则 2/8 完全没拦住它——机制级修复同样可以是发散的。规则:

1. **修复引入的每个新概念(新类型/新协议段/新表/新层),必须在修复说明中列出并回答"谁消费它的值"。只被存在性检查消费、或只被"读它的测试"消费 = 删除。**
2. **连续两轮新增概念 = 方案发散**,等同原则 7 的 diff 发散,触发原则 8 停机判定。停机后的产出优先做减法(合并重复机制、删除不被消费的层),不是设计更统一的新框架。
3. **静态枚举审计(原则 6)不得演化为与行为平行的结构机制**:契约表+生成器是枚举审计的恶性肿瘤形态——它验证"表与解析器一致",不验证"面板动作对"。#144 八轮中每个真缺陷都由行为测试或 reviewer 动态复现抓获,结构护栏拦截数为零。证明层必须断言**最终行为**(动作/unknown/计数器恒等式),结构一致性断言只作补充。
4. **并发回归写交错,不写直线**:反序完成、跨代 join、缓存投毒回归全部由 reviewer 的交错复现逼出;顺序执行的测试证明不了时序正确。

配套三条(#144 全程验证有效):

- **迁移类改动保留 legacy 路径作等价基准**,直到迁移关闭。"零语义变化"由 deepEqual 对基准断言,不由口头承诺。
- **L3 默认切法 = 零行为变化铺路 PR + 机制 PR**:切片 A(纯提取,现有断言零修改)一轮通过;B+C 合并交付后进入八轮循环。铺路 PR 同时产出等价基准。
- **评审进行中不是冻结错误的理由**:为观测者保留一个已知错误的前提,是流程便利凌驾正确性,而且通常连观测者的指令都读反了——#144 的 Observer 指令("删除并行机制")恰恰要求那次的减法。

## 机器门禁(已实现:`pnpm run check:state-writes`,AST 解析)

脚本 `scripts/check-state-writes.mjs` 基于 TypeScript Program + TypeChecker 做**符号级解析**(非文本正则、非名字集合):每个调用表达式解析到 state/workflow-persistence 的真实导出符号,别名链、命名空间属性、const/let 重绑定、赋值重绑定与解构均按 symbol 追踪。负向 fixture(`tests/check-state-writes-gate.test.ts`)锁定全部已知绕过路径——门禁自身也有必须失败的测试(名字追踪版本曾在两轮 review 中被绕过,故换范式)。已接入 `check` 复合链与 CI,执行原则 2/4 的三条边界规则:

1. **状态路径白名单**:workflow 状态文件路径(`workflowPath`/`workflowStatePath`)只允许在持久化层解析(`workflow-persistence.ts` 写入、`state-layout.ts` 定义、`state.ts` facade);其他文件触及即 CI 红。路径独占同时保证写入只能是持久化层的 temp + rename 原子写。
2. **持久化模块独占**:`workflow-persistence.ts` 只允许被 `state.ts`(facade)与 `task-ownership.ts`(纯选择器)import;上层直接 import 即 CI 红。
3. **条件提交**:任何 `saveWorkflow`/`saveWorkflowForTask`/`claimWorkflowTask`/`mutateWorkflowForTask` 的无 revision/凭证参数调用(单参数裸调用 = 无条件 last-writer-wins)即 CI 红。

> 定位:门禁是**绊线(heuristic tripwire)**,不是边界。语法级追踪原理上无法证明能力边界(值可经对象属性、高阶参数等任意形态传播,#111 三轮绕过实证);真正的边界 = 模块私有/API 面不暴露 raw mutator(构造级)。raw mutator 移除后,本门禁降级为回归绊线保留。

## 适用范围

- **强制**:涉及共享可变状态、任务/进程生命周期、持久化文件、autoRun 控制器的修复与新功能。
- **参照**:纯展示、纯查询类改动可豁免原则 1-6,但原则 7-8(流程元规则)对所有 review 循环生效。

## 首次接入(整套采纳)

#111 的教训:协议零件全部"疼了才装",每件晚于本该出现的时机 3-8 轮,合计多付约 15 轮。新项目/新仓库应在第一天整套接入:

1. 本文档 + `skills/root-cause-review/SKILL.md` + `skills/observer/SKILL.md` 一并放入仓库;
2. AGENTS.md 与 CLAUDE.md 各加一行强制引用(两个 agent 入口都要通);
3. 指派停机规则的**触发人**(每轮 review 出现后核对原则 8 触发条件;无人核对 = 规则不存在);
4. 首个涉及共享状态的改动动手前先写不变量文档(原则 1);
5. 循环稳定后做"裸点实验"(不贴任何指令直接 review),验证协议是否自驱动;不自驱动则把 skill 接入产品的 prompt 构建。

## 循环驱动模板

人工驱动循环时,输入可以只有一句:**「按 docs/fix-discipline.md〈某轮〉模板执行」**。场景由 review 判定决定(fix-these → 修复轮;stop-and-redesign → 重设计轮;新 issue → 新开发)。

### 新开发(首次开发,无 review 历史)

> 按 docs/fix-discipline.md 开发。若涉及共享状态、任务生命周期或持久化:先在 issue 评论写下不变量(原则 1)再动手写代码;实现优先机制级(原则 2/4,写权限即凭证);交付物包含受影响 mutation 路径的静态枚举表(原则 6)。纯展示/查询类改动可豁免原则 1-6。提交前自检:若发现自己在多个调用点插入相同检查,停下来换机制级方案。

### 修复轮(review 判定 = fix-these)

> 读 docs/fix-discipline.md 与本轮 review 后修复。先对每个 finding 判母题:若属于历史已复发母题,或唯一可行的修法是调用点级,禁止逐条修,按原则 8 报告需要重设计轮。单发新母题按原则 2 选机制级修法,并更新静态枚举表;回复中列出本轮新增概念及其消费者(原则 11),只被存在性检查消费的概念直接删除。证明层断言最终行为(动作/unknown/计数),并发回归写交错不写直线。本轮 diff 应净减少或持平(原则 7)。

### 重设计轮(review 判定 = stop-and-redesign)

> 重设计轮。先产出不变量文档,再按原则 2/4 实现条件提交:每条 workflow mutation 的签名要求 capability 或 expected revision,锁内重读-验证-变更-原子提交或拒绝;任务启动把 reservation + taskId + hostJobId + stage 建立为一次线性化迁移;移除所有无条件整对象写调用点。交付物必须包含静态枚举表:全部 mutation 路径逐条列出、逐条标注保护方式,表中不得有「调用点纪律」或「无保护」行。同步处理 review 指出的前瞻问题(如锁重入、错误语义混叠)。本轮 diff 允许净减少。

### 循环驱动(状态:已接线)

模板的消费方是 ClickVibe 自身的 prompt 构建;本文档是模板的唯一事实源,代码侧引用而非复制。

> 实现状态:**已接线**(2026-08-25,直接合入 main)。`src/agent/prompt.ts` 的 `parseReviewMetaVerdict`/`reworkRoundDirective` 解析 Review Meta 的 verdict/next/theme 字段:reviewer 附「下一轮指令」段时以其为准;stop-and-redesign 判定注入〈重设计轮〉模板指针;普通失败注入〈修复轮〉模板指针(旧格式 Review Meta 兼容)。`buildReviewPrompt` 首条要求内联根因 review 协议精简版,并声明"仓库存在 skills/root-cause-review/SKILL.md 或本文档时以其全文为准"。人工输入「按 fix-discipline〈某轮〉模板执行」从必需品降级为可选强化。

## 关联

- 完整复盘(三阶段时间线、协议演化、六条教训):`docs/plans/2026-08-25-issue-111-retrospective.md`
- 案例全记录:issue #111 及 PR #114 的 18 条 Review Meta 评论;原则 11 案例:PR #144 的 Review 评论链(八轮 + Observer 干预 + 奥卡姆收敛)。
- 并发模型与任务所有权设计:`docs/state-model.md`「宿主重启与任务恢复」。
- review 执行流程:`skills/root-cause-review/SKILL.md`。
